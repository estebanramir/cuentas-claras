# Solo para desarrollo local. En un build o una actualizacion publicada, el
# valor por defecto de app.json (la API en produccion) es el que manda.
#
# 10.0.2.2 es como el emulador de Android ve el localhost de la maquina.
EXPO_PUBLIC_API_URL=http://10.0.2.2:4000
